# AdotaPet — Frontend (Vite + React)

Aplicação web. Configure `VITE_API_BASE_URL` apontando para a API (repositório do backend).

## Comandos

```bash
npm install
npm run dev
```

## Variáveis

Copie `.env.example` para `.env` e ajuste a URL base da API.
